import test from "node:test";
import assert from "node:assert/strict";
import {createInitialState} from "../src/core/store.js";
import {checksum,migrate} from "../src/data/save-manager.js";
test("checksum changes when state changes",()=>{const a=createInitialState("A"),before=checksum(a);a.economy.available--;assert.notEqual(checksum(a),before)});
test("legacy save migrates without deleting campaign data",()=>{const old={meta:{saveVersion:1},campaign:{act:4},colony:{},ui:null,ship:{design:{propulsion:"nuclear"}},crew:{members:["amara","jun","elias","asha","malik","lena"],training:1},mission:{flightStage:0}};const next=migrate(old);assert.equal(next.meta.saveVersion,8);assert.equal(next.campaign.act,4);assert.equal(next.campaign.turn,1);assert.equal(next.ui.tutorialEnabled,true);assert.deepEqual(next.colony.research,[]);assert.deepEqual(next.engineering.locked,["propulsion"]);assert.equal(Object.values(next.crew.assignments).filter(Boolean).length,6);assert.equal(next.crew.trainingModules.length,3);assert.equal(next.crew.legacyCertified,true);assert.equal(next.engineering.recoveryCount,0);assert.equal(next.mission.orbit.body,"Terra");assert.equal(next.astronomy.selectedBody,"earth");assert.ok(next.science)});
